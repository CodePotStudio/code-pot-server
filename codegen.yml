overwrite: true
schema:
  - "./src/graphql/typeDefs/**.types.ts"
documents: null
generates:
  src/@types/graphql.d.ts:
    mapper:
      User: ./user#User
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: ./context#Context
      scalars:
        Date: String
